# Design notes

hello_stats.py demonstrates the five disciplines of a "good script": stdlib-only, a
`__main__` guard, a self-describing `--help`, machine-readable `--json`, and error paths that
exit with code 2 plus a readable message.

The golden output (in `expected_outputs/`) is generated by `--json` and is what script_tester compares against.
