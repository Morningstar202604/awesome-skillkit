我需要先了解一下你的项目环境，才能生成可直接运行的测试套件。请提供以下信息：

1. **项目根目录** — 包含 `POST /orders` 路由定义的代码路径
2. **技术栈** — 框架（Next.js / Express / FastAPI / Django）+ 测试栈（Vitest+Supertest 或 Pytest+httpx）

如果你不确定框架，我可以先用探测命令扫描。收到信息后我会按以下步骤执行：

- 扫描路由定义，提取 `/orders` 的处理逻辑、校验规则、鉴权要求
- 生成完整测试文件，覆盖你要求的维度：

| 维度 | 覆盖要点 |
|---|---|
| 正常 | sku 有效 + qty 有效 + 可选 coupon 有效 → 201 |
| 边界 | qty=1（最小）、qty=max（上限）、coupon 为空字符串/缺失 |
| 异常 | sku 不存在、qty≤0、qty 超库存、coupon 无效/过期 |
| 幂等 | 相同 idempotency-key 重复提交 → 201（非重复创建） |
| 并发 | 同一 sku 库存扣减不超卖 |

用例将以表格形式呈现，并生成对应代码。